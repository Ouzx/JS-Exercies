const firstYearCompleted = true;
const yearsCompleted = 1;

// double equal operator will convert the value to the same type and then compare
// triple equal operator will not convert the value to the same type and then compare
console.log(yearsCompleted == firstYearCompleted);
console.log(yearsCompleted === firstYearCompleted);
